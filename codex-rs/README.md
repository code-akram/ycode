# ycode Rust workspace

The supported product entry point is the `codex` binary from `codex-cli` on
`aarch64-apple-darwin`. Build, validation, and installation commands are
documented in [../docs/install.md](../docs/install.md).
